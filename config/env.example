# Claude n8n Bridge Server Configuration
# Copy this file to .env and update the values

# API key for securing communication between n8n and the bridge server
# Generate a secure random string and use the same value in your n8n HTTP Request node
BRIDGE_API_KEY=change-me-to-something-secret

# Port for the bridge server (default: 3456)
PORT=3456
